# Phase 7 — LLMs: system prompt, user prompt, input, output

Each Phase file uses **`Current`** (approved baseline, used when coding after a prompt that includes **`execute`**) and **`Changes`** (staging from **`#plan`**, typically **blockquoted**). See `../Project Execution.md`.

---

## Current

> If no LLMs: **N/A** — state that explicitly and note which downstream sections are unused.

### LLM 1 — *[Name / call site]*

**Model:** *(e.g. gpt-4o)* — **Temperature:**

#### System prompt

#### User prompt template

#### Input (contract)

| Field | Type |
|-------|------|

#### Output (contract)

| Field | Type |
|-------|------|

---

### LLM 2 — *[Name / call site]*

*(Repeat block.)*

---

### Links to other Phases

---

## Changes

> **Staged for `#plan`** — highlighted for review vs **Current**. Merged into **Current** when the user runs **`execute`**.
>
> *(None yet.)*
